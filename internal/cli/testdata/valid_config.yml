---
server:
  host: "0.0.0.0"
  port: 8060
storage:
  provider: "s3"
  s3:
    bucket: "local-artifacts"
    region: "us-east-1"
metadata:
  postgres:
    host: "localhost"
    port: 5432
    name: "latent"
    user: "user"
    password: "password"
coordinator:
  valkey:
    endpoints: ["localhost:6379"]
